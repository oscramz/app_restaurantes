<h1>Listado de Restaurantes disponibles</h1>

<%= link_to "Agregar Restaurante", nuevo_restaurante_path %>

<table class="table table-hover">
    <tr>
        <th>Restaurante</th>
        <!-- <th>Especialidad</th> -->
        <th>Acciones</th>
    </tr>

<% @todos_restaurantes.each do |restaurante| %>
    <tr>
        <td>
            <!-- <%= link_to restaurantes_path(restaurante) do %>
             <%= restaurante.nombre %>
             <% end %> -->
             <%= link_to restaurante.nombre, restaurante_path(restaurante) %>
        </td>
        <!-- <td><%= restaurante.tipo_comida.tipo %></td> -->
        <td>
            <%= link_to "Editar", editar_restaurantes_path(restaurante) %>

            <%= link_to "Borrar", restaurantes_path(restaurante), method: :delete, data: { confirm: '¿Está seguro que quiere eliminar este restaurante?' } %>
        </td>
    </tr>
<% end %>

</table>